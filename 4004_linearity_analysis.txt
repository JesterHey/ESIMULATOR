============================================================
DFG 线性/非线性运算分析报告
============================================================

📊 运算类型统计:
  线性运算绑定: 5 个
  非线性运算绑定: 0 个
  控制逻辑绑定: 0 个
  存储更新绑定: 10 个

🔵 线性运算部分:
  • alu.n0345
    运算: Operator(And, [IntConst(4'b1000)])
    复杂度: 线性运算1个, 深度1
  • alu.n0354
    运算: Operator(And, [IntConst(4'b0100)])
    复杂度: 线性运算1个, 深度1
  • alu.n0363
    运算: Operator(And, [IntConst(4'b0010)])
    复杂度: 线性运算1个, 深度1
  • alu.n0370
    运算: Operator(And, [IntConst(4'b0001)])
    复杂度: 线性运算1个, 深度1
  • alu.n0377
    运算: Operator(Or, [Operator(Eq, [])])
    复杂度: 线性运算1个, 深度1

🟢 存储更新部分:
  • alu.acc (寄存器)
    更新逻辑: Branch
  • alu.acc_out (寄存器)
    更新逻辑: Branch
  • alu.cy (寄存器)
    更新逻辑: Branch
  • alu.cy_1 (寄存器)
    更新逻辑: Branch
  • alu.dout (寄存器)
    更新逻辑: Branch
  • alu.n0550 (寄存器)
    更新逻辑: Branch
  • alu.n0749 (寄存器)
    更新逻辑: Branch
  • alu.n0750 (寄存器)
    更新逻辑: Branch
  • alu.n0751 (寄存器)
    更新逻辑: Branch
  • alu.tmp (寄存器)
    更新逻辑: Branch

============================================================
详细Verilog代码段
============================================================

### Linear Section:
1. assign n0345 = And(4'b1000);
2. assign n0354 = And(4'b0100);
3. assign n0363 = And(4'b0010);
4. assign n0370 = And(4'b0001);
5. assign n0377 = Or(Eq());


### Storage Section:
1. assign acc = (add_acc ? acc_in : 1'b0);
2. assign acc_out = (n0854 ? acc : 1'b0);
3. assign cy = (And() ? 1'b1 : (adc_cy ? n0861 : 1'b0));
4. assign cy_1 = (n0854 ? cy : 1'b0);
5. assign dout = (n0415 ? _rn4_dout : (cy_ib ? _rn3_dout : (add_ib ? _rn2_dout : (acb_ib ? _rn1_dout : _rn0_dout))));
6. assign n0550 = (cy_ada ? cy : 1'b0);
7. assign n0749 = (poc ? 1'b1 : (n0351 ? n0355 : 1'b0));
8. assign n0750 = (poc ? 1'b1 : (n0351 ? n0364 : 1'b0));
9. assign n0751 = (poc ? 1'b1 : (n0351 ? n0371 : 1'b0));
10. assign tmp = (m12 ? 4'b1111 : 1'b0);
