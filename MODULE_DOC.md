# 模块解说文档

本文档旨在详细解释 `dfg_linearity_corrector.py` 模块各部分的原理、结构和作用，以便后人阅读、理解和改写。

---

## 1. 模块整体概述

该模块用于对描述数据流图（DFG）的文本文件进行解析和处理，主要任务包括：

- **解析 DFG 表达式**：通过构建简单的 AST（抽象语法树）来解析括号驱动的 S 表达式，避免纯正则匹配的脆弱性。
- **评估线性性**：递归遍历 AST，按照预定义的线性运算符集合，判断各个信号表达式是否为线性表达式。
- **报告生成**：统计各类表达式的数量、类型、复杂度以及非线性原因，生成详细的分析报告。

---

## 2. 解析器部分

### 2.1 辅助函数

- **`_skip_ws(s, i)`**：
  - 功能：跳过字符串 `s` 中从索引 `i` 开始的所有空白字符，并返回新索引。
  - 作用：确保解析时忽略无关空格。

- **`_read_token(s, i, stop_chars)`**：
  - 功能：从字符串中读取一个 token，直到遇到指定的停止字符（例如 `(`, `)`, `:`, `,` 等）。
  - 作用：用于提取各部分关键词，如 `Terminal`、`Operator` 等。

- **`_extract_parenthesized(s, i)`**：
  - 功能：从索引 `i` 开始，提取一对完整括号内的子串，并返回子串及下一个索引位置。
  - 作用：实现基于括号计数的结构化拆分，确保匹配正确关闭的括号。

### 2.2 主解析函数：`parse_expression(s, i)`

- **作用**：递归解析表达式字符串，构建一个 `ExpressionNode` 类型的 AST 节点。
- **支持的表达式类型**：
  - *Terminal*：直接赋值，判定为线性。
  - *IntCon/IntConst*：整数常量，判定为线性。
  - *Operator*：运算表达式，通过解析操作符及其子表达式构建子树。
  - *Concat*：拼接表达式，分解并解析各个子表达式。
  - *Branch*：条件分支表达式，解析条件、真值和假值子表达式（默认判定为非线性）。
  - *Partselect*：位选择表达式，主要关注变量部分（通常 MSB 和 LSB 为常量）。

- **实现原理**：
  - 利用前面辅助函数确定节点类型。
  - 根据关键字（如 `Operator`、`Concat` 等）分支递归调用自身，构造完整 AST。
  - 在解析过程中，如果格式不符，则抛出 `ParseError` 异常。

### 2.3 辅助解析函数

- **`_split_top_exprs(s)`**：
  - 功能：利用括号弧度统计按逗号分隔顶层子表达式，确保不会误拆嵌套表达式。

- **`_extract_tagged_subexpr(whole, tag, required)`**：
  - 功能：从整体字符串中提取带有特定标签（如 `Cond:`、`True:`、`False:` ）后的括号表达式。

---

## 3. AST 线性判定部分

### 3.1 函数 `evaluate_linearity(node, linear_ops)`

- **作用**：递归遍历 AST 节点并根据节点类型及其运算符判断该节点是否为线性表达式。

- **判断规则**：
  - *Terminal 和 Constant*：一律判定为线性。
  - *Operator*：如果操作符在预定义的线性运算符集合中，并且所有子节点均为线性，则整体为线性；否则，返回非线性及原因信息（如非线性算子名称）。
  - *Concat*：要求所有子表达式均是线性，才判定为线性。
  - *Partselect*：主要查看其变量部分的线性性。
  - *Branch*：默认判定为非线性，也可扩展条件常量情形。

- **意义**：通过树的深度遍历，可以更精确地捕获子表达式的关系、层次及其对整体线性性的影响。

---

## 4. CorrectedLinearityAnalyzer 类

### 4.1 构造函数

- 定义了两个集合：
  - **`linear_operators`**：基本的线性运算符（如 `Plus`, `Minus`, `Concat` 等）。
  - **`nonlinear_operators`**：逻辑、归约、乘除、比较和位移等运算符，默认视为非线性。

- 初始化统计信息和信号分析字典。

### 4.2 文件分析流程

- **`analyze_dfg_file(file_path)`**：
  - 读取 DFG 文件内容。
  - 使用正则表达式粗略分割各个 Bind 表达式作为信号表达式的基础单元。
  - 针对每个 Bind 表达式，根据表达式前缀选择不同处理分支：
    - 对于 Terminal 和 IntCon 类型，直接赋予简单的线性结果。
    - 对于其他表达式，尝试利用 AST 解析和 `evaluate_linearity` 判断线性性；若解析失败则回退到旧方案。
  - 最后生成汇总报告（包括统计总数、线性/非线性比例、各表达式类型分布以及运算符使用统计等）。

---

## 5. 生成报告部分

- **`_generate_comprehensive_report()`**：
  - 对所有解析结果进行统计，构建一个结构化报告字典。
  - 包括信号数、复杂度分布、表达式类型分布、非线性原因和运算符使用频率。
  - 分析结果被写入文本报告文件中，供后续参考。

---

## 6. 主函数及使用示例

- **`analyze_real_dfg(file_name)`**：
  - 模块入口，指定待分析的 DFG 文件名（如 `4004_dfg.txt`）。
  - 调用 `CorrectedLinearityAnalyzer` 进行整体处理，并生成控制台输出和分析报告文件。

- 使用方法：

```sh
python src/analyzers/dfg_linearity_corrector.py
```

---

## 7. 后续扩展方向


- 进一步完善 AST 的解析和错误管理，以确保针对各种表达式格式能有更鲁棒的解析结果。
- 根据项目需求扩展线性判定策略（例如不同的数学域下的线性定义）。
- 提高报告中统计维度（如表达式深度、非线性触发节点个数等），以便更精细的分析。
